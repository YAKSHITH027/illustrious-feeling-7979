var fs = new Splide( '#first-slider', {
    type : 'loop',
    arrows : true,
    autoplay : true,
} ).mount();

var ss = new Splide( '#second-slider', {
    type : 'loop',
    arrows : true,
    autoplay : true,
}).mount();